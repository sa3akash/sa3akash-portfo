import React from "react";
import styled from "styled-components";
import Circle from "./Circle";

const Services = () => {
  return (
    <ServicesContainer className="section bd_grid">
      <Circle />
    </ServicesContainer>
  );
};

export default Services;

const ServicesContainer = styled.div``;
